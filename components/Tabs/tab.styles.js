import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  tab: {
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default styles;
